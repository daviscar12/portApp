import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'portApp--template=blank',
  webDir: 'dist'
};

export default config;
